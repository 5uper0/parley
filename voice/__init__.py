"""Optional voice layer for Parley — I/O around the unchanged consensus core.

Not part of the zero-dependency core (`parley/`). The gateway is an OpenAI-compatible endpoint
that makes Parley the brain of an ElevenLabs Custom-LLM agent; a Claude-backed NL extractor drops
in behind an optional extra later.
"""
